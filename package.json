{
  "name": "@quankaiws/theo-gameframe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic multiplayer game platform and Discord Activity foundation for Theo.",
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "dev": "node --experimental-strip-types src/server/http-server.ts",
    "test": "node scripts/run-tests.mjs",
    "validate": "npm test && node --experimental-strip-types src/scripts/self-check.ts"
  }
}
