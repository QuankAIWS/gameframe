{
  "name": "@quankaiws/scribbles-gameframe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic multiplayer game platform and Discord Activity foundation for the Scribbles architecture.",
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "dev": "node --experimental-strip-types src/server/http-server.ts",
    "dev:cloudflare": "wrangler dev",
    "test": "node scripts/run-tests.mjs",
    "test:workerd": "vitest run --config vitest.config.ts --max-workers=1 --no-isolate",
    "test:browser": "playwright test --config playwright.config.mjs",
    "validate": "npm test && npm run test:workerd && npm run check:browser && npm run test:browser && node --experimental-strip-types src/scripts/self-check.ts",
    "check:browser": "node --check public/gameframe-auth.js && node --check public/auth-launcher.js && node --check public/app.js && node --check public/tactical-app.js && node --check public/combat-app.js"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.19.0",
    "@playwright/test": "1.61.1",
    "vitest": "4.1.10",
    "wrangler": "4.115.0"
  }
}
