{
  "name": "@quankaiws/scribbles-gameframe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Deterministic multiplayer game platform and Discord Activity foundation for the Scribbles architecture.",
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "dev": "node --experimental-strip-types src/server/http-server.ts",
    "dev:cloudflare": "wrangler dev",
    "build:activity": "node scripts/build-activity-bundle.mjs",
    "check:activity": "node scripts/build-activity-bundle.mjs --check",
    "test": "node scripts/run-tests.mjs",
    "test:workerd": "vitest run --config vitest.config.ts --max-workers=1 --no-isolate",
    "test:browser": "playwright test --config playwright.config.mjs",
    "test:visual": "playwright test --config playwright.visual.config.mjs",
    "test:visual-baseline": "playwright test --config playwright.visual-baseline.config.mjs",
    "validate": "npm test && npm run test:workerd && npm run check:activity && npm run check:browser && npm run test:browser && npm run test:visual-baseline && node --experimental-strip-types src/scripts/self-check.ts",
    "check:browser": "node --check public/discord-activity-bootstrap.js && node --check public/gameframe-auth.js && node --check public/secure-match-invite.js && node --check public/invite-app.js && node --check public/auth-launcher.js && node --check public/app.js && node --check public/game-polish.js && node --check public/tactical-app.js && node --check public/combat-app.js && node --check public/monster-master-motion.js && node --check public/monster-master-app.js"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.19.0",
    "@discord/embedded-app-sdk": "2.5.0",
    "@playwright/test": "1.61.1",
    "esbuild": "0.28.1",
    "vitest": "4.1.10",
    "wrangler": "4.115.0"
  }
}
