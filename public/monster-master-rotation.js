// The legacy Canvas projection is intentionally disabled.
// Monster Master now loads the committed PixiJS battlefield bundle through auth-launcher.js.
window.gameFrameMonsterLegacyProjection = Object.freeze({ disabled: true });
